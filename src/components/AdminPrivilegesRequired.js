import { Typography } from '@material-ui/core';

const AdminPrivilegesRequired = () => {
	return (
		<div>
			<Typography variant="h6" style={{ textAlign: 'center' }}>
				Admin privileges required
			</Typography>
		</div>
	);
};

export default AdminPrivilegesRequired;
